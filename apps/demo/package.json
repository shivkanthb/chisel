{
  "name": "chisel-demo",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "build": "echo 'no build needed'"
  },
  "dependencies": {
    "chisel-engine": "^0.1.0",
    "chisel-studio": "^0.1.1"
  }
}
